component{
    this.name = "myApp";

    // Default Datasource Name
    this.datasources = {
            // Adobe Driver Approach
            db = {
                driver: "PostgreSQL",
                database : "cfml_its",
                host : "127.0.0.1",
                port : "5432",
                username : "user"
            }
            };
}
